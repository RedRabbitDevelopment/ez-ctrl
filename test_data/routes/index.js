// Generated by CoffeeScript 1.7.1
(function() {
  var BaseController, ErrorHandler, MyBaseController, Q, UserData, base, beforeEach, data, middleware;

  Q = require('q');

  base = require('../../index');

  BaseController = base.BaseController;

  data = require('../data');

  middleware = data.middleware;

  beforeEach = data.beforeEach;

  UserData = data.Userdata;

  ErrorHandler = data.ErrorHandler;

  module.exports = MyBaseController = BaseController.extend({
    isAbstract: true,
    beforeEach: function() {
      return beforeEach.myBaseRan++;
    },
    middleware: function(req, res, next) {
      middleware.myBaseRan++;
      return next();
    }
  });

  MyBaseController.prototype.logError = function(error) {
    return ErrorHandler.logError(error);
  };

}).call(this);
